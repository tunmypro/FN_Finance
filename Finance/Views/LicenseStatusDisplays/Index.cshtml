@model IEnumerable<Finance.Models.LicenseStatusDisplay>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LicenseStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LicenseStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LicenseStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LicenseStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LicenseStatusID })
        </td>
    </tr>
}

</table>
